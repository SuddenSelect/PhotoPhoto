<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="pl.edu.pw.mmajews2.photophoto.MainActivity"
    android:background="@color/colorBackground">

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/settingsButton"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:src="@drawable/ic_settings_black_24dp"
        android:scaleType="fitCenter"
        android:onClick="launchSettingActivity"
        android:background="@color/colorBackground"
        android:tint="@android:color/holo_blue_dark"
        android:layout_alignParentStart="false"
        android:padding="10dp" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/takePictureButton"
        android:src="@drawable/ic_camera_alt_black_24dp"
        android:scaleType="fitCenter"
        android:onClick="launchTakePictureActivity"
        android:layout_alignParentEnd="true"
        android:layout_alignParentStart="true"
        android:layout_above="@+id/space"
        android:layout_alignParentTop="true"
        android:background="@color/colorBackground"
        android:tint="@android:color/holo_green_light"
        android:paddingTop="40dp" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/decodePictureButton"
        android:src="@drawable/ic_lock_open_black_24dp"
        android:scaleType="fitCenter"
        android:onClick="launchDecodePictureActivity"
        android:layout_above="@+id/settingsButton"
        android:layout_alignParentEnd="true"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/space"
        android:background="@color/colorBackground"
        android:tint="@android:color/holo_orange_dark" />

    <Space
        android:layout_width="20px"
        android:layout_height="20px"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:id="@+id/space" />

</RelativeLayout>
